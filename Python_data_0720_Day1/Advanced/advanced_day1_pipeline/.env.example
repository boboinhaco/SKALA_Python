# .env 템플릿 (이 파일만 깃에 커밋). 실제 URL은 .env에 채워 넣는다.
#   cp .env.example .env  후 각 값 입력
# ※ Open-Meteo(open api)와 ip-api의 실제 URL은 깃/인터넷에 올리지 않는다.

OPENMETEO_URL=<open-meteo forecast URL 입력>
COUNTRIES_URL=https://countries.dev/alpha/KOR
IPAPI_URL=<ip-api URL 입력>
